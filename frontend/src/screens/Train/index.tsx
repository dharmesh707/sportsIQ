import { Text, View } from 'react-native';

/**
 * TODO (teammate 3): migrate the real Train screen UI here from the
 * old BadmintonIQ repo, per the brief's "Repo strategy" section — cherry-pick
 * the component, don't rebuild from scratch.
 */
export default function TrainScreen() {
  return (
    <View>
      <Text>Train — TODO: migrate from BadmintonIQ</Text>
    </View>
  );
}
