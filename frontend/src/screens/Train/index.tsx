/**
 * TODO (teammate 3): build the real Train screen UI here from scratch.
 */
export default function TrainScreen() {
  return (
    <View>
      <Text>Train — TODO: build fresh, see PROJECT_README.md</Text>
    </View>
  );
}
