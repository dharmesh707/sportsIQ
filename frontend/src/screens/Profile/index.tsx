/**
 * TODO (teammate 3): build the real Profile screen UI here from scratch.
 */
export default function ProfileScreen() {
  return (
    <View>
      <Text>Profile — TODO: build fresh, see PROJECT_README.md</Text>
    </View>
  );
}
