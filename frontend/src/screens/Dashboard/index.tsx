import { Text, View } from 'react-native';

/**
 * TODO (teammate 3): migrate the real Dashboard screen UI here from the
 * old BadmintonIQ repo, per the brief's "Repo strategy" section — cherry-pick
 * the component, don't rebuild from scratch.
 */
export default function DashboardScreen() {
  return (
    <View>
      <Text>Dashboard — TODO: migrate from BadmintonIQ</Text>
    </View>
  );
}
